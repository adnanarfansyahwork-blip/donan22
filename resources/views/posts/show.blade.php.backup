@extends('layouts.app')

@section('title', ($post->meta_title ?? $post->title) . ' - Donan22')
@section('meta_description', $post->meta_description ?? $post->excerpt)
@section('meta_keywords', $post->meta_keywords)
@section('og_title', $post->meta_title ?? $post->title)
@section('og_description', $post->meta_description ?? $post->excerpt)
@section('og_image', $post->featured_image_url)
@section('og_type', 'article')

@section('content')
<article class="py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Main Content -->
            <div class="lg:col-span-2">
                <!-- Breadcrumb -->
                <nav class="flex items-center space-x-2 text-sm text-gray-500 mb-6">
                    <a href="{{ route('home') }}" class="hover:text-primary-600">Home</a>
                    <i class="bi bi-chevron-right text-xs"></i>
                    @if($post->postType)
                        <a href="{{ route($post->postType->slug . '.index') }}" class="hover:text-primary-600">{{ $post->postType->name }}</a>
                        <i class="bi bi-chevron-right text-xs"></i>
                    @endif
                    @if($post->category)
                        <a href="{{ route('categories.show', $post->category->slug) }}" class="hover:text-primary-600">{{ $post->category->name }}</a>
                        <i class="bi bi-chevron-right text-xs"></i>
                    @endif
                    <span class="text-gray-900 line-clamp-1">{{ $post->title }}</span>
                </nav>
                
                <!-- Header -->
                <header class="mb-8">
                    <div class="flex flex-wrap gap-2 mb-4">
                        @if($post->postType)
                            <span class="text-xs font-medium bg-primary-100 text-primary-700 px-3 py-1 rounded-full">
                                <i class="bi {{ $post->postType->icon }} mr-1"></i> {{ $post->postType->name }}
                            </span>
                        @endif
                        @if($post->category)
                            <span class="text-xs font-medium bg-gray-100 text-gray-700 px-3 py-1 rounded-full">
                                {{ $post->category->name }}
                            </span>
                        @endif
                        @if($post->is_featured)
                            <span class="text-xs font-medium bg-yellow-100 text-yellow-700 px-3 py-1 rounded-full">
                                <i class="bi bi-star-fill"></i> Featured
                            </span>
                        @endif
                    </div>
                    
                    <h1 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4">
                        {{ $post->title }}
                    </h1>
                    
                    <div class="flex flex-wrap items-center gap-4 text-sm text-gray-500">
                        <div class="flex items-center">
                            <img src="{{ $post->user->avatar_url }}" alt="{{ $post->user->name }}" class="w-8 h-8 rounded-full mr-2">
                            <span>{{ $post->user->name }}</span>
                        </div>
                        <span><i class="bi bi-calendar mr-1"></i> {{ $post->published_at?->format('M d, Y') }}</span>
                        <span><i class="bi bi-eye mr-1"></i> {{ number_format($post->views_count) }} views</span>
                        <span><i class="bi bi-clock mr-1"></i> {{ $post->reading_time }} min read</span>
                    </div>
                </header>
                
                <!-- Featured Image -->
                <figure class="mb-8 text-center bg-gradient-to-br from-gray-50 to-gray-100 rounded-2xl p-4 shadow-lg">
                    <img src="{{ $post->featured_image_url }}" 
                         alt="{{ $post->featured_image_alt ?? $post->title }}" 
                         class="max-w-full max-h-[280px] object-contain mx-auto rounded-xl shadow-md">
                </figure>
                
                <!-- Content -->
                <div class="prose prose-lg max-w-none mb-8">
                    {!! $post->content !!}
                </div>
                
                <!-- Tags -->
                @if($post->tags instanceof \Illuminate\Support\Collection && $post->tags->count())
                    <div class="flex flex-wrap gap-2 mb-8 pt-6 border-t">
                        <span class="text-gray-500 text-sm"><i class="bi bi-tags mr-1"></i> Tags:</span>
                        @foreach($post->tags as $tag)
                            <a href="{{ route('search', ['tag' => $tag->slug]) }}" 
                               class="text-sm bg-gray-100 hover:bg-gray-200 text-gray-700 px-3 py-1 rounded-full transition-colors">
                                {{ $tag->name }}
                            </a>
                        @endforeach
                    </div>
                @endif
                
                <!-- Share Buttons -->
                <div class="flex items-center gap-4 mb-8 pt-6 border-t">
                    <span class="text-gray-500 text-sm">Share:</span>
                    <a href="https://www.facebook.com/sharer/sharer.php?u={{ urlencode($post->url) }}" target="_blank" 
                       class="w-10 h-10 flex items-center justify-center bg-blue-600 hover:bg-blue-700 text-white rounded-full transition-colors">
                        <i class="bi bi-facebook"></i>
                    </a>
                    <a href="https://twitter.com/intent/tweet?url={{ urlencode($post->url) }}&text={{ urlencode($post->title) }}" target="_blank"
                       class="w-10 h-10 flex items-center justify-center bg-black hover:bg-gray-800 text-white rounded-full transition-colors">
                        <i class="bi bi-twitter-x"></i>
                    </a>
                    <a href="https://wa.me/?text={{ urlencode($post->title . ' ' . $post->url) }}" target="_blank"
                       class="w-10 h-10 flex items-center justify-center bg-green-500 hover:bg-green-600 text-white rounded-full transition-colors">
                        <i class="bi bi-whatsapp"></i>
                    </a>
                    <button onclick="navigator.clipboard.writeText('{{ $post->url }}')" 
                            class="w-10 h-10 flex items-center justify-center bg-gray-200 hover:bg-gray-300 text-gray-700 rounded-full transition-colors">
                        <i class="bi bi-link-45deg"></i>
                    </button>
                </div>
                
                <!-- Comments Section -->
                @if($post->allow_comments)
                    <section class="pt-8 border-t">
                        <h2 class="text-2xl font-bold text-gray-900 mb-6">
                            <i class="bi bi-chat-dots mr-2"></i> Comments ({{ $post->comments_count }})
                        </h2>
                        
                        <!-- Comment Form -->
                        <form action="{{ route('posts.comment', $post) }}" method="POST" class="mb-8 bg-gray-50 rounded-xl p-6">
                            @csrf
                            @guest
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
                                    <input type="text" name="guest_name" placeholder="Your Name" required
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
                                           value="{{ old('guest_name') }}">
                                    <input type="email" name="guest_email" placeholder="Your Email" required
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
                                           value="{{ old('guest_email') }}">
                                </div>
                            @endguest
                            
                            <textarea name="content" rows="4" placeholder="Write your comment..." required
                                      class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500 mb-4">{{ old('content') }}</textarea>
                            
                            <button type="submit" class="px-6 py-3 bg-primary-600 hover:bg-primary-700 text-white font-medium rounded-lg transition-colors">
                                <i class="bi bi-send mr-2"></i> Submit Comment
                            </button>
                        </form>
                        
                        <!-- Comments List -->
                        @if($comments->count())
                            <div class="space-y-6">
                                @foreach($comments as $comment)
                                    @include('components.comment', ['comment' => $comment])
                                @endforeach
                            </div>
                            
                            <div class="mt-6">
                                {{ $comments->links() }}
                            </div>
                        @else
                            <p class="text-gray-500 text-center py-8">No comments yet. Be the first to comment!</p>
                        @endif
                    </section>
                @endif
            </div>
            
            <!-- Sidebar -->
            <aside class="lg:col-span-1 space-y-6">
                <!-- Software Details Card -->
                @if($post->softwareDetail)
                    <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
                        <h3 class="text-lg font-bold text-gray-900 mb-4">
                            <i class="bi bi-info-circle mr-2 text-primary-600"></i> Details
                        </h3>
                        
                        <dl class="space-y-3 text-sm">
                            @if($post->softwareDetail->version)
                                <div class="flex justify-between">
                                    <dt class="text-gray-500">Version</dt>
                                    <dd class="font-medium text-gray-900">{{ $post->softwareDetail->version }}</dd>
                                </div>
                            @endif
                            @if($post->softwareDetail->developer)
                                <div class="flex justify-between">
                                    <dt class="text-gray-500">Developer</dt>
                                    <dd class="font-medium text-gray-900">
                                        @if($post->softwareDetail->developer_url)
                                            <a href="{{ $post->softwareDetail->developer_url }}" target="_blank" class="text-primary-600 hover:underline">
                                                {{ $post->softwareDetail->developer }}
                                            </a>
                                        @else
                                            {{ $post->softwareDetail->developer }}
                                        @endif
                                    </dd>
                                </div>
                            @endif
                            @if($post->softwareDetail->platform)
                                <div class="flex justify-between">
                                    <dt class="text-gray-500">Platform</dt>
                                    <dd class="font-medium text-gray-900 capitalize">{{ $post->softwareDetail->platform_label }}</dd>
                                </div>
                            @endif
                            @if($post->softwareDetail->file_size)
                                <div class="flex justify-between">
                                    <dt class="text-gray-500">Size</dt>
                                    <dd class="font-medium text-gray-900">{{ $post->softwareDetail->file_size }}</dd>
                                </div>
                            @endif
                            @if($post->softwareDetail->license_type)
                                <div class="flex justify-between">
                                    <dt class="text-gray-500">License</dt>
                                    <dd class="font-medium text-gray-900">{{ $post->softwareDetail->license_type }}</dd>
                                </div>
                            @endif
                            <div class="flex justify-between">
                                <dt class="text-gray-500">Price</dt>
                                <dd class="font-medium {{ $post->softwareDetail->is_free ? 'text-green-600' : 'text-gray-900' }}">
                                    {{ $post->softwareDetail->price_formatted }}
                                </dd>
                            </div>
                        </dl>
                        
                        @if($post->softwareDetail->official_website)
                            <a href="{{ $post->softwareDetail->official_website }}" target="_blank" 
                               class="block mt-4 text-center text-sm text-primary-600 hover:text-primary-700">
                                <i class="bi bi-box-arrow-up-right mr-1"></i> Official Website
                            </a>
                        @endif
                    </div>
                @endif
                
                <!-- Download Links -->
                @if($post->downloadLinks->count())
                    <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
                        <h3 class="text-lg font-bold text-gray-900 mb-4">
                            <i class="bi bi-download mr-2 text-green-600"></i> Download
                        </h3>
                        
                        <div class="space-y-3">
                            @foreach($post->downloadLinks as $link)
                                <a href="{{ route('posts.download', [$post, $link]) }}" 
                                   class="flex items-center justify-between p-3 bg-gray-50 hover:bg-primary-50 rounded-lg border border-gray-200 hover:border-primary-300 transition-colors group"
                                   target="_blank">
                                    <div class="flex items-center">
                                        <i class="bi {{ $link->provider_icon }} text-xl text-gray-500 group-hover:text-primary-600 mr-3"></i>
                                        <div>
                                            <div class="font-medium text-gray-900 group-hover:text-primary-600">{{ $link->name }}</div>
                                            @if($link->file_size)
                                                <div class="text-xs text-gray-500">{{ $link->file_size }}</div>
                                            @endif
                                        </div>
                                    </div>
                                    <i class="bi bi-arrow-right text-gray-400 group-hover:text-primary-600"></i>
                                </a>
                            @endforeach
                        </div>
                        
                        @if($post->downloadLinks->first()?->password)
                            <div class="mt-4 p-3 bg-yellow-50 border border-yellow-200 rounded-lg">
                                <div class="text-xs text-yellow-700 font-medium mb-1">Password:</div>
                                <code class="text-sm text-yellow-800 bg-yellow-100 px-2 py-1 rounded">{{ $post->downloadLinks->first()->password }}</code>
                            </div>
                        @endif
                        
                        <p class="mt-4 text-xs text-gray-500 text-center">
                            <i class="bi bi-shield-check mr-1"></i> All files are scanned for viruses
                        </p>
                    </div>
                @endif
                
                <!-- Related Posts -->
                @if($relatedPosts->count())
                    <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
                        <h3 class="text-lg font-bold text-gray-900 mb-4">
                            <i class="bi bi-collection mr-2 text-primary-600"></i> Related
                        </h3>
                        
                        <div class="space-y-4">
                            @foreach($relatedPosts as $related)
                                <a href="{{ $related->url }}" class="flex gap-3 group">
                                    <div class="w-16 h-16 flex-shrink-0 bg-gray-100 rounded-lg overflow-hidden">
                                        <img src="{{ $related->featured_image_url }}" 
                                             alt="{{ $related->title }}" 
                                             class="w-full h-full object-cover">
                                    </div>
                                    <div class="flex-1 min-w-0">
                                        <h4 class="font-medium text-gray-900 text-sm line-clamp-2 group-hover:text-primary-600 transition-colors">
                                            {{ $related->title }}
                                        </h4>
                                        <div class="text-xs text-gray-500 mt-1">
                                            <i class="bi bi-eye mr-1"></i> {{ number_format($related->views_count) }}
                                        </div>
                                    </div>
                                </a>
                            @endforeach
                        </div>
                    </div>
                @endif
            </aside>
        </div>
    </div>
</article>
@endsection

@push('styles')
<style>
    .prose img {
        border-radius: 0.5rem;
    }
    .prose a {
        color: #2563eb;
    }
    .prose code {
        background: #f3f4f6;
        padding: 0.2rem 0.4rem;
        border-radius: 0.25rem;
        font-size: 0.875em;
    }
    .prose pre {
        background: #1f2937;
        border-radius: 0.5rem;
    }
</style>
@endpush


